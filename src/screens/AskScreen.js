import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AskScreen = () => {
	return <Text>This is Ask Screen</Text>;
};

const styles = StyleSheet.create({});

export default AskScreen;
